# dependencies
node_modules/

# database (persistent data — lives on the host volume, not in git)
data/
*.db
*.db-shm
*.db-wal

# owner-uploaded content images (user data — not part of the source)
public/images/uploads/

# os / editor cruft
.DS_Store
*.log

# local env
.env
.env.local
